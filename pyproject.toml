[project]
name = "fraudia-claims"
version = "1.0.1"
requires-python = ">=3.11,<3.13"
dependencies = []

[tool.vercel]
# FastAPI (Python ASGI) en app.py
entrypoint = "app:app"

[tool.vercel.scripts]
build = "python scripts/vercel_build.py"
