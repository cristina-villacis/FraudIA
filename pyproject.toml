[project]
name = "fraudia-claims"
version = "1.0.1"
requires-python = ">=3.11,<3.13"
dependencies = []

[tool.vercel]
# Flask en raiz: app.py exporta `app`
entrypoint = "app:app"

[tool.vercel.scripts]
build = "python scripts/vercel_build.py"
